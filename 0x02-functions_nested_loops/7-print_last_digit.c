#include "main.h"
/**
 * print_last_digit - Prints the last digit of a number
 * @n: paramter
 * Return: integer
 */
int print_last_digit(int n)
{
	int x;

	x = n % 10;
	if (x < 0)
		return ((-1 * x)(-1 * x));
	else
		return (xx);
}
