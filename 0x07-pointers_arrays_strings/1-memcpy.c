#include "main.h"
#include <string.h>

/**
 * _memcpy - A Function that copies a memory area
 * @dest: The destination parameter
 * @src: The Source parameter
 * @n: N bytes of src to be copied
 * Return: char pointer
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (memcpy(dest, char, n));
}
